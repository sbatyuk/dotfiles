set --local GIT_NAME  "Sam Hosseini"
set --local GIT_EMAIL "git-email@hosseini.io"

set --global --export GIT_AUTHOR_NAME       $GIT_NAME
set --global --export GIT_COMMITTER_NAME    $GIT_NAME

set --global --export GIT_AUTHOR_EMAIL      $GIT_EMAIL
set --global --export GIT_COMMITTER_EMAIL   $GIT_EMAIL
